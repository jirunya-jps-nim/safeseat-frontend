import React from 'react'

// ─── Register Page Styles (Light Theme) ───────────────────────────
export const registerStyles: Record<string, React.CSSProperties> = {
  page: {
    minHeight: '100vh',
    background: 'linear-gradient(135deg, #f8fafc 0%, #f1f5f9 50%, #e2e8f0 100%)',
    display: 'flex',
    flexDirection: 'column',
    position: 'relative',
    overflow: 'hidden',
    fontFamily: "'Prompt', sans-serif",
  },
  bgCircle1: {
    position: 'absolute',
    top: '-180px',
    right: '-120px',
    width: '500px',
    height: '500px',
    borderRadius: '50%',
    background: 'radial-gradient(circle, rgba(99,102,241,0.06) 0%, transparent 70%)',
    pointerEvents: 'none',
  },
  bgCircle2: {
    position: 'absolute',
    bottom: '-80px',
    left: '-160px',
    width: '420px',
    height: '420px',
    borderRadius: '50%',
    background: 'radial-gradient(circle, rgba(124,58,237,0.05) 0%, transparent 70%)',
    pointerEvents: 'none',
  },
  bgCircle3: {
    position: 'absolute',
    top: '40%',
    left: '42%',
    width: '300px',
    height: '300px',
    borderRadius: '50%',
    background: 'radial-gradient(circle, rgba(59,130,246,0.03) 0%, transparent 70%)',
    pointerEvents: 'none',
  },

  // ─── Layout ───────────────────────────────────────────────────
  main: { 
    flex: 1, 
    display: 'flex', 
    position: 'relative', 
    zIndex: 5,
    maxWidth: '1200px',
    margin: '40px auto',
    width: 'calc(100% - 40px)',
    background: '#ffffff',
    borderRadius: '20px',
    boxShadow: '0 10px 30px rgba(0, 0, 0, 0.04), 0 1px 3px rgba(0, 0, 0, 0.02)',
    border: '1px solid #e2e8f0',
    overflow: 'hidden',
  },

  // ─── Hero (Left) ──────────────────────────────────────────────
  heroPanel: {
    width: '40%',
    position: 'relative',
    overflow: 'hidden',
    flexShrink: 0,
  },
  heroImage: { width: '100%', height: '100%', objectFit: 'cover', display: 'block' },
  heroOverlay: {
    position: 'absolute',
    inset: 0,
    background:
      'linear-gradient(160deg, rgba(79,70,229,0.9) 0%, rgba(124,58,237,0.95) 100%)',
  },
  heroContent: {
    position: 'absolute',
    inset: 0,
    zIndex: 2,
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'flex-end',
    padding: '32px 28px',
  },
  heroBadge: {
    display: 'inline-block',
    background: 'rgba(255,255,255,0.18)',
    backdropFilter: 'blur(6px)',
    border: '1px solid rgba(255,255,255,0.25)',
    color: '#ffffff',
    fontSize: 11.5,
    fontWeight: 500,
    padding: '5px 14px',
    borderRadius: 16,
    marginBottom: 14,
    width: 'fit-content',
  },
  heroTitle: {
    color: '#fff',
    fontSize: 24,
    fontWeight: 700,
    lineHeight: 1.35,
    margin: '0 0 10px',
    textShadow: '0 1px 4px rgba(0,0,0,0.15)',
  },
  heroDesc: {
    color: 'rgba(255,255,255,0.85)',
    fontSize: 12.5,
    lineHeight: 1.7,
    margin: '0 0 24px',
  },
  heroSteps: {
    display: 'flex',
    flexDirection: 'column',
    gap: 0,
    background: 'rgba(255,255,255,0.12)',
    border: '1px solid rgba(255,255,255,0.18)',
    borderRadius: 14,
    padding: '16px 18px',
    backdropFilter: 'blur(10px)',
  },
  heroStep: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    position: 'relative',
    paddingBottom: 4,
  },
  heroStepDot: {
    width: 36,
    height: 36,
    borderRadius: '50%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexShrink: 0,
    transition: 'all 0.3s',
  },
  heroStepLabel: { fontSize: 13, lineHeight: 1.2 },
  heroStepNum: { fontSize: 10.5, color: 'rgba(255,255,255,0.5)', marginTop: 1 },
  heroStepLine: {
    position: 'absolute',
    left: 17,
    top: 40,
    width: 2,
    height: 24,
    borderRadius: 2,
    transition: 'background-color 0.3s',
  },

  // ─── Form (Right) ─────────────────────────────────────────────
  formPanel: {
    flex: 1,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: '24px 0',
    backgroundColor: '#ffffff',
  },
  formInner: {
    width: '100%',
    maxWidth: 420,
    padding: '0 48px',
    animation: 'fadeUp 0.4s ease both',
  },
  formHeader: { textAlign: 'center', marginBottom: 20 },
  formIconWrap: {
    width: 50,
    height: 50,
    borderRadius: 14,
    background: 'rgba(99,102,241,0.08)',
    border: '1px solid rgba(99,102,241,0.2)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    margin: '0 auto 12px',
  },
  formTitle: { fontSize: 22, fontWeight: 700, color: '#0f172a', margin: '0 0 4px' },
  formSubtitle: { fontSize: 12.5, color: '#64748b', margin: 0 },

  // ─── Progress Bar ─────────────────────────────────────────────
  progressTrack: {
    height: 4,
    borderRadius: 4,
    backgroundColor: '#e2e8f0',
    marginBottom: 22,
    overflow: 'hidden',
  },
  progressFill: {
    height: '100%',
    borderRadius: 4,
    background: 'linear-gradient(90deg, #4f46e5, #7c3aed)',
    transition: 'width 0.4s ease',
  },

  // ─── Input ────────────────────────────────────────────────────
  input: {
    width: '100%',
    padding: '10px 14px',
    borderRadius: '8px',
    border: '1px solid #cbd5e1',
    fontSize: '13.5px',
    color: '#0f172a',
    backgroundColor: '#ffffff',
    outline: 'none',
    boxSizing: 'border-box',
    fontFamily: "'Prompt', sans-serif",
    marginBottom: 0,
    transition: 'border-color 0.2s, box-shadow 0.2s',
  },

  // ─── Feedback ─────────────────────────────────────────────────
  errorBox: {
    background: '#fef2f2',
    border: '1px solid #fca5a5',
    borderRadius: 9,
    color: '#991b1b',
    fontSize: 13,
    padding: '9px 14px',
    marginBottom: 12,
  },

  // ─── Navigation Buttons ───────────────────────────────────────
  btnRow: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 16,
    gap: 12,
  },
  backBtn: {
    background: '#ffffff',
    border: '1px solid #cbd5e1',
    color: '#475569',
    padding: '11px 22px',
    borderRadius: 8,
    cursor: 'pointer',
    fontSize: 14,
    fontWeight: 500,
    fontFamily: "'Prompt', sans-serif",
    transition: 'all 0.2s',
  },
  nextBtn: {
    flex: 1,
    background: '#4f46e5',
    border: 'none',
    color: '#fff',
    padding: '12px 24px',
    borderRadius: 8,
    cursor: 'pointer',
    fontSize: 14,
    fontWeight: 600,
    fontFamily: "'Prompt', sans-serif",
    transition: 'all 0.2s',
  },
}
